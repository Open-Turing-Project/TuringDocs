{
  "title": "strrealok",
  "fileName": "strrealok",
  "sections": [
    {
      "title": "Syntax",
      "raw_content": "<font size=\"+1\"><b>strrealok</b> ( <i>s</i> : <b>string</b> ) : <b>boolean</b><p>\r\n</p></font>",
      "text_content": " strrealok ( s : string ) : boolean\r\n",
      "mdown_content": "**strrealok** ( *s* : **string** ) : **boolean**"
    },
    {
      "title": "Description",
      "raw_content": "The <b>strrealok</b> function is used determine whether the <b>strreal</b> function can be used to convert the string to a real number without causing an error. If the string can be successfully converted, then <b>strrealok</b> returns <b>true</b>, otherwise it returns <b>false</b>.<p>\r\nString <i>s</i> should consist of a possibly null sequence of blanks, then an optional plus or minus sign and finally an explicit unsigned real or integer constant. If it does so, then <b>strrealok</b> will return <b>true</b>, otherwise it returns <b>false</b>.</p>\n<p>\r\n</p>",
      "text_content": " The strrealok function is used determine whether the strreal function can be used to convert the string to a real number without causing an error. If the string can be successfully converted, then strrealok returns true, otherwise it returns false.\r\nString s should consist of a possibly null sequence of blanks, then an optional plus or minus sign and finally an explicit unsigned real or integer constant. If it does so, then strrealok will return true, otherwise it returns false.\r\n",
      "mdown_content": "The **strrealok** function is used determine whether the **strreal** function can be used to convert the string to a real number without causing an error. If the string can be successfully converted, then **strrealok** returns **true**, otherwise it returns **false**.\nString *s* should consist of a possibly null sequence of blanks, then an optional plus or minus sign and finally an explicit unsigned real or integer constant. If it does so, then **strrealok** will return **true**, otherwise it returns **false**."
    },
    {
      "title": "See also",
      "links": [
        "strreal"
      ],
      "raw_content": "<b><a href=\"strreal.html\">strreal</a></b> function that does the actual conversion.<p>\r\n</p>",
      "text_content": " strreal function that does the actual conversion.\r\n",
      "mdown_content": "**[strreal.html](strreal)** function that does the actual conversion."
    }
  ],
  "dependencies": [

  ]
}
